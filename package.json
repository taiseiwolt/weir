{
  "name": "aiden-demo",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@supabase/supabase-js": "^2.49.1",
    "openai": "^4.73.0",
    "stripe": "^17.5.0"
  },
  "scripts": {
    "embeddings": "node scripts/generate-embeddings.js",
    "test": "npx playwright test --config=playwright.config.cjs",
    "test:headed": "npx playwright test --config=playwright.config.cjs --headed",
    "lint": "grep -rn 'console\\.log' --include='*.html' --include='*.js' api/ supabase/functions/ *.html 2>/dev/null; if [ $? -eq 0 ]; then echo '\\n❌ console.log found — remove before commit' && exit 1; else echo '✅ No console.log found' && exit 0; fi",
    "build": "echo 'No build step required'"
  },
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}

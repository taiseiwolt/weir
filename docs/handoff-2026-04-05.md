# 引き継ぎ資料：2026年4月4日深夜〜4月5日午前

## 1. iTerm2セットアップ（CC並列運用環境）

- Homebrew確認済み → iTerm2インストール完了
- 3ペイン分割で運用開始（左1 + 右上1 + 右下1）
- 各ペインの起動コマンド: `cd ~/Desktop/aiden-demo && claude --dangerously-skip-permissions`
- 日本語入力OK、全バイパスモードで確認不要

## 2. いきなりステーキ ブランドHP修正

### 2-1. ブランド切り替え不具合（修正済み）
- **原因**: `BRAND_SLUG = 'sumibite-shibuya'` がハードコードされており、URLパラメータ `?brand=` を一切読んでいなかった
- **修正**: `resolveBrandId()` 関数を追加し、`?brand=` パラメータから動的にブランドを解決するよう変更

### 2-2. 修正の巻き戻り事故（修正済み）
- **原因**: CC-Aがフッター修正をコミット → CC-B（ブランド切り替え修正）が古いスナップショットからファイルを再作成 → CC-Aの修正が消失
- **復元した5項目**:
  1. ヘッダー丸型ロゴ削除
  2. フッター丸型ロゴ削除
  3. フッター「ニュース・キャンペーン」配置確認（巻き戻りなし）
  4. フッター会社情報（株式会社ペッパーフードサービス / 東京都墨田区緑2-14-15）表示復元
  5. 店舗写真（Supabase Storage外観写真）表示復元

### 2-3. ブランドデータ分離（修正済み）
- お案内セクション: brand_idでフィルタし、炭火亭データの混在を解消
- フッター説明文: brands.brand_descriptionから動的取得に変更
- フッターレイアウト: 「会社情報」と「ニュース・キャンペーン」を横並び（grid 5カラム）に変更

### 2-4. フッター追加修正（修正済み）
- 「運営会社:」「所在地:」ラベルプレフィックス削除 → 社名・住所のみ表示
- 株式会社ペッパーフードサービスの折り返し解消（gridカラム幅調整）
- 「© 2026 炭火亭 All rights reserved.」削除
- 「Powered by Weir」削除
- プライバシーポリシー / 利用規約 / サイトマップ → 中央揃えに変更

## 3. Git競合防止ルール（CLAUDE.md更新済み）

### 巻き戻り事故の再発防止策としてCLAUDE.mdに追加：
- **Gitセクション**: 「Git 競合防止ルール」新設
  - 作業開始前に `git pull origin main` 必須
  - 編集前に `git log --oneline -5 <対象ファイル>` で直近変更確認
  - コミット前に `git diff HEAD` で自分の変更のみ含まれているか確認
  - push前に `git pull --rebase origin main` 実行
  - 同一ファイルの並列編集禁止
- **Before Starting Any Task**: 1番目に `git pull origin main` 追加
- **After Completing Any Task**: push前の `git pull --rebase` と `git push` 追加

### .gitignore修正：
- `.env*.local` → `.env*` に変更（全.envファイルをgit管理外に）

## 4. GitHub Secret Scanning & APIキーローテーション

### 経緯：
- 過去のコミット（1a7160e）に .env.temp / .env.vercel / .env.prod 内のAPIキーが含まれていた
- git pushがGitHub Push Protectionでブロックされた

### 対応：
- GitHub Secret Scanningの3件（Anthropic / OpenAI / Stripe）を「I'll fix it later」で許可
- 3社とも漏洩を検知して自動対応:
  - Anthropic: 自動でキー無効化
  - OpenAI: 自動でキー無効化
  - Stripe: 7日後に自動無効化の警告
- 3つのAPIキーを全て再発行済み
  - Anthropic: https://console.anthropic.com/settings/keys で新規発行
  - OpenAI: Restricted権限（Images: Requestのみ）で新規発行
  - Stripe: 既存キーをローテーション
- Vercel環境変数を新しいキーに更新 → 再デプロイ完了

## 5. Phase 0: Admin Master Tables（未完了）

### 実装済み（CC完了）：
- fee_schedules / user_bans / brand_permissions / platform_settings / menu_patterns テーブル作成
- audit_logs 拡張（6カラム追加 + インデックス3件）
- fee_schedules 初期データ（全法人×3チャネル）
- platform_settings 初期データ（4件）
- stripe-create-payment-intent Edge Function 動的料率取得対応

### 未完了：
- **SQLマイグレーション実行**: 1つ目のSQL（テーブル作成）でエラー発生
  - エラー: `column "brand_id" does not exist` — menu_patternsのRLSポリシーでstaff_accountsテーブルのbrand_idを参照しているが存在しない
  - CCに修正依頼済み → **修正版SQLの結果待ち**
- 2つ目のSQL（seed data）は1つ目の成功後に実行する
- Edge Functionデプロイ: `supabase functions deploy stripe-create-payment-intent` 未実施

## 6. データ一括管理機能（デプロイ済み・動作確認済み）
- Admin: サイドバー「データ一括管理」タブ + BULK_TEMPLATES定義（6種別）
- Customer Admin: NAV「一括登録」+ CA_BULK_TEMPLATES定義
- API: 認証なしアクセスで401返却確認済み
- 全ページ表示確認OK

## 7. 残タスク一覧

| # | タスク | ステータス | 備考 |
|---|--------|-----------|------|
| 1 | Phase 0 SQL修正版の実行 | ⏳ CC修正版待ち | brand_idエラーの修正 |
| 2 | Phase 0 seed SQL実行 | ⏳ #1完了後 | |
| 3 | stripe-create-payment-intent EFデプロイ | ⏳ #1完了後 | |
| 4 | いきなりステーキ お案内セクション写真2枚目が空白 | 未確認 | mediaテーブルのデータ確認必要 |
| 5 | APIキー再発行後の動作確認 | 未実施 | DALL-E POP画像生成、Claude API等 |
